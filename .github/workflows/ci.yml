name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    types:
      - synchronize
      - opened
      - ready_for_review
      - reopened

# Cancel in-flight runs for the same branch/PR when a new commit arrives.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  COVERAGE_THRESHOLD: 70

jobs:
  dependency-review:
    name: Dependency Review
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/dependency-review-action@v4
        with:
          fail-on-severity: high       # block PRs that add High/Critical CVEs
          deny-licenses: GPL-2.0, AGPL-3.0

#  lint:
#    name: Lint
#    runs-on: ubuntu-latest
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v4
#
#      - name: Set up Go
#        uses: actions/setup-go@v5
#        with:
#          go-version: '1.25'
#          cache: true
#          cache-dependency-path: go.sum
#
#      - name: Run golangci-lint
#        uses: golangci/golangci-lint-action@v4
#        with:
#          version: v2.4.0
#          args: --timeout=5m

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.26.4'
          cache: true
          cache-dependency-path: go.sum

      - name: Generate mocks
        run: |
          go install go.uber.org/mock/mockgen@latest
          go generate ./internal/...

      - name: Run govulncheck
        # govulncheck checks the actual call graph, not just the module graph —
        # much lower false-positive rate than pure dependency scanners.
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          govulncheck ./...

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26'
          cache: true

      - name: Download dependencies
        run: go mod download

      - name: Generate mocks
        run: |
          go install go.uber.org/mock/mockgen@latest
          go generate ./internal/...

      - name: Run tests with coverage
        run: go test -v -race -coverprofile=coverage.out -covermode=atomic ./...

      - name: Calculate coverage
        id: coverage
        run: |
          total=$(go tool cover -func=coverage.out | grep total | grep -Eo '[0-9]+\.[0-9]+')
          total_integer=${total%.*}
          echo "total=${total}" >> "${GITHUB_OUTPUT}"
          echo "total_integer=${total_integer}" >> "${GITHUB_OUTPUT}"
          echo "Coverage: ${total}%"

      - name: Enforce coverage threshold
        if: matrix.go-version == '1.26.x'
        run: |
          total="${{ steps.coverage.outputs.total_integer }}"
          if [ "$total" -lt "${{ env.COVERAGE_THRESHOLD }}" ]; then
            echo "::error::Coverage ${total}% is below the required threshold of ${{ env.COVERAGE_THRESHOLD }}%"
            exit 1
          fi
          echo "Coverage ${total}% meets the threshold of ${{ env.COVERAGE_THRESHOLD }}%"
      - name: Comment coverage on PR
        if: github.event_name == 'pull_request' && matrix.go-version == '1.26.x'
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          message: |
            ## Code Coverage Report
            Coverage: **${{ steps.coverage.outputs.total }}%** (threshold: ${{ env.COVERAGE_THRESHOLD }}%)
      - name: Upload coverage to Codecov
        if: matrix.go-version == '1.26.x'
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage.out
          flags: unittests
          fail_ci_if_error: false

  test-integration:
    name: Integration Tests
    runs-on: ubuntu-latest
    needs: [test]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.26.x'
          cache: true
          cache-dependency-path: go.sum

      - name: Generate mocks
        run: |
          go install go.uber.org/mock/mockgen@latest
          go generate ./internal/...    

      - name: Run integration tests
        run: go test -v -race -tags integration -timeout 60s ./internal/api/...
        env:
          # Integration tests use these to construct real JWTs
          JWT_SECRET: "a-test-secret-that-is-exactly-32-bytes-long"
          JWT_ISSUER: "synapse-platform"
          JWT_AUDIENCE: "synapse-api"

  docker:
    name: Docker Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lint Dockerfile
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: Dockerfile
          # Ignore specific rules if you have justified exceptions.
          # ignore: DL3008,DL3009
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build image (validation only — no push)
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          tags: synapse-platform:ci
          # Export the build cache to GitHub's cache — speeds up subsequent builds.
          cache-from: type=gha
          cache-to: type=gha,mode=max

  infra:
    name: Infrastructure Validation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.26'
          cache: true
          cache-dependency-path: go.sum

      - name: Validate docker-compose.yaml
        # docker compose config resolves all anchors, substitutions, and
        # extension fields, then validates the result against the spec.
        run: docker compose -f docker-compose.yaml config --quiet
      - name: Validate sqlc configuration
        run: |
          go install github.com/sqlc-dev/sqlc/cmd/sqlc@latest
          sqlc vet

  helm-lint:
    name: Helm Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: azure/setup-helm@v4

      - name: Build Helm dependencies
        run: helm dependency build infra/helm/synapse-platform/

      - name: Lint Synapse Helm chart
        run: helm lint infra/helm/synapse-platform/

  helm-template:
    name: Helm Template Validation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: azure/setup-helm@v4

      - name: Install kubeconform
        run: |
          curl -sSL https://github.com/yannh/kubeconform/releases/download/v0.6.7/kubeconform-linux-amd64.tar.gz \
            | tar -xz kubeconform
          sudo mv kubeconform /usr/local/bin/kubeconform

      - name: Build Helm dependencies
        run: helm dependency build infra/helm/synapse-platform/

      - name: Render and validate manifests
        run: |
          helm template synapse-platform infra/helm/synapse-platform/ \
            --namespace synapse-platform \
            -f infra/helm/synapse-platform/values-dev.yaml \
            | kubeconform -

  terraform-fmt:
    name: Terraform Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3

      - name: Check Terraform formatting
        run: terraform fmt -check -recursive infra/terraform

  terraform-validate:
    name: Terraform Validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3

      - name: Initialize local environment
        run: terraform -chdir=infra/terraform/environments/local init -backend=false

      - name: Validate local environment
        run: terraform -chdir=infra/terraform/environments/local validate

      - name: Initialize cloud-dev environment
        run: terraform -chdir=infra/terraform/environments/cloud-dev init -backend=false

      - name: Validate cloud-dev environment
        run: terraform -chdir=infra/terraform/environments/cloud-dev validate

  api-contract:
    name: OpenAPI Contract
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Validate OpenAPI document
        run: npx --yes @redocly/cli@latest lint openapi.yaml

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [security, test, docker, infra, helm-lint, helm-template, terraform-fmt, terraform-validate, api-contract]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.26.x'
          cache: true
          cache-dependency-path: go.sum
      - name: Build binary
        run: go build -v -ldflags="-w -s" -o bin/synapsePlatform ./cmd/
      - name: Upload binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: synapsePlatform-binary
          path: bin/synapsePlatform
          retention-days: 1

      - name: Write CI summary
        if: always()
        run: |
          echo "## CI Pipeline Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Check | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|---|---|" >> $GITHUB_STEP_SUMMARY
          echo "| Security scan | ✅ Passed |" >> $GITHUB_STEP_SUMMARY
          echo "| Unit tests | ✅ Passed |" >> $GITHUB_STEP_SUMMARY
          echo "| Integration tests | ✅ Passed |" >> $GITHUB_STEP_SUMMARY
          echo "| Docker build | ✅ Passed |" >> $GITHUB_STEP_SUMMARY
          echo "| Infrastructure | ✅ Passed |" >> $GITHUB_STEP_SUMMARY
          echo "| Binary | \`bin/synapsePlatform\` |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Branch:** \`${{ github.ref_name }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Triggered by:** ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
