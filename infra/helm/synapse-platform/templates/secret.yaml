apiVersion: v1
kind: Secret
metadata:
  name: {{ include "synapse-platform.fullname" . }}-secret
  labels:
    {{- include "synapse-platform.labels" . | nindent 4 }}
type: Opaque
data:
  jwt-secret: {{ required "auth.jwt.secret is required" .Values.auth.jwt.secret | b64enc | quote }}